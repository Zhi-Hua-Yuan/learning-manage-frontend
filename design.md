# FocusFlow v2（中文场景落地版）

面向大学生的 AI + TODO 应用设计系统。目标是保持简洁、年轻、可执行，同时确保中文可读性和交互闭环。

---

## 1. 设计目标与边界

### 1.1 目标
- 让用户可以在 10 秒内看懂页面层级，30 秒内创建并推进任务。
- AI 功能作为“增强器”，不抢占任务主流程。
- 保持风格统一：清晰、轻量、有活力，但不过度炫技。

### 1.2 暂不纳入（避免过度设计）
- 一期不做扇形 AI 悬浮球。
- 一期不同时上线看板、日历、专注模式三套重交互视图。
- 一期不做依赖外部课表数据的“智能出发提醒”。

---

## 2. 色彩系统

### 2.1 主色与语义色
- 主色：`#2563EB`（Blue 600）
- 主色悬停：`#1D4ED8`（Blue 700）
- 主色浅底：`#DBEAFE`（Blue 100）
- 成功：`#10B981`（Emerald 500）
- 警告：`#F59E0B`（Amber 500）
- 危险：`#EF4444`（Red 500）
- AI 强调：`#8B5CF6`（Violet 500）

### 2.2 中性色
- 标题：`#111827`（Gray 900）
- 正文：`#374151`（Gray 700）
- 次要信息：`#6B7280`（Gray 500）
- 分割线：`#E5E7EB`（Gray 200）
- 页面背景：`#F9FAFB`（Gray 50）
- 卡片背景：`#FFFFFF`

---

## 3. 字体与排版（中文优先）

### 3.1 字体策略
- UI 主字体（中文优先）：
  `"Noto Sans SC", "PingFang SC", "Microsoft YaHei", "Inter", -apple-system, "Segoe UI", sans-serif`
- 数字/计时/统计（局部使用）：
  `"JetBrains Mono", "SFMono-Regular", "Consolas", "Noto Sans Mono CJK SC", monospace`

说明：`JetBrains Mono` 不作为全站主字体，仅用于数字信息，避免中文 fallback 不一致。

### 3.2 字号与字重
| 层级 | 大小 | 字重 | 用途 |
|------|------|------|------|
| 页面标题 | 24px | 600 | 页面主标题 |
| 区块标题 | 18px | 600 | 卡片头、模块名 |
| 正文 | 14-15px | 400 | 任务标题、描述 |
| 辅助文本 | 12-13px | 400 | 标签、时间、说明 |
| 徽标文本 | 12px | 500 | 状态徽章、微标签 |

规则：中文界面不使用 11px 作为常规可读文本。

---

## 4. 布局与信息架构

### 4.1 主结构（沿用现有产品模型）
- 左侧：项目/功能导航（任务、数据仪表盘、周报、AI 规划、设置）。
- 中间：任务列表与分组（里程碑）。
- 右侧：任务详情编辑面板（桌面端）。

### 4.2 间距与圆角
- 8px 栅格：`4 / 8 / 16 / 24 / 32 / 48`
- 圆角：
  - 小元素：6px
  - 输入框/按钮/任务项：10px
  - 大卡片/弹窗：16px

---

## 5. 核心组件规范

### 5.1 任务项（列表优先，轻卡片化）
- 背景白色，1px 灰边框，圆角 10px。
- Hover：轻阴影 + `translateY(-1px)`。
- 完成态：
  - 文本减弱 + 删除线；
  - 勾选块使用成功色（`#10B981`），而非主蓝色。
- AI 建议任务：左侧 3px 紫色标记（`#8B5CF6`）。

### 5.2 复选框
- 尺寸：20px。
- 未选中：白底 + `#D1D5DB` 边框。
- 悬停：边框转主蓝。
- 选中：填充成功色 + 白色对勾。
- 动效：`0.15s` scale easing。

### 5.3 优先级
低优先级不可隐藏，必须可见。

| 等级 | 颜色 | 形态 |
|------|------|------|
| 紧急 | `#EF4444` | 实心圆点 + “紧急” |
| 高 | `#F59E0B` | 半实心圆点 + “高” |
| 中 | `#6B7280` | 空心圆点 + “中” |
| 低 | `#9CA3AF` | 细描边圆点 + “低” |

---

## 6. AI 功能融合策略（不破坏主流程）

### 6.1 入口策略
- 一期入口：
  - 左侧导航中的 `AI 规划`（主入口）
  - 任务详情里的“AI 建议”按钮（上下文入口）
- 不采用：
  - 右下角扇形悬浮球（与移动端底部导航冲突，且易遮挡内容）

### 6.2 AI 交互闭环
- 智能拆解：先生成“草稿预览”，允许用户勾选后再导入。
- 时间估算：显示为辅助字段（例如 `⏱ 45 分钟`），不直接覆盖用户设置。
- 每日回顾：晚间摘要卡片可作为周报入口，不强打断。

---

## 7. 关键交互逻辑

### 7.1 高风险操作
- 删除任务/阶段/项目：二次确认。
- 提供撤销窗口（Toast 5 秒内 Undo）优先于仅弹窗确认。

### 7.2 保存与反馈
- 自动保存字段：输入框 blur 后保存，失败时明确提示并可重试。
- 成功反馈统一：右上角 Toast，3 秒自动消失。

### 7.3 动效控制
- 页面切换：`0.2s fade + 0.1s slide`。
- 避免同时叠加多个抢眼动画（脉冲 + 抖动 + 位移）。
- 所有动画支持 `prefers-reduced-motion` 降级。

---

## 8. 响应式策略

- 桌面端（`>=1024`）：
  - 左侧固定导航 + 中间内容 + 右侧详情。
- 平板端（`768-1023`）：
  - 右侧详情改抽屉；
  - 统计/周报从三列降为两列。
- 手机端（`<768`）：
  - 单列任务；
  - 底部 Tab 导航；
  - AI 入口保留在 Tab 或页面内按钮，不使用悬浮球遮挡输入区。

---

## 9. 文案与中文语境规范

- 避免过度“玩梗”文案，优先清晰行动导向。
- 错误提示要有下一步动作建议（如“请检查网络后重试”）。
- 时间/日期遵循中文习惯：`YYYY-MM-DD` 或 `M 月 D 日`，同页统一。

---

## 10. 实施分期（建议）

### P0（先做，低风险高收益）
- 全局设计 token（颜色/字体/圆角/阴影）。
- 任务页复选框与优先级语义统一。
- 关键页面补齐基础响应式断点。

### P1（稳定后）
- AI 草稿勾选导入流程。
- 周报与任务数据的关联展示（例如本周完成任务来源）。

### P2（验证需求后）
- 看板/日历/专注模式逐个上线（每次只上一个）。

---

## 11. 与当前代码落点对应

- 全局样式：`src/assets/main.css`
- 布局与导航：`src/layout/BasicLayout.vue`
- 任务主流程：`src/views/task/TaskList.vue`
- AI 规划：`src/views/ai/AiPlanner.vue`
- 周报：`src/views/review/WeeklyReview.vue`
- 仪表盘：`src/views/dashboard/Dashboard.vue`

以上作为实现参考基线。若产品方向变化，可在此文档上增补“变更记录”。

