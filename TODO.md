# 前端改造 TODO（基于 design.md v2）

## 使用说明
- 状态说明：`[ ]` 未开始，`[~]` 进行中，`[x]` 已完成。
- 执行顺序：先完成 `P0`，验收通过后进入 `P1`，最后评估 `P2`。

---

## P0：低风险高收益（先做）

### 1. 建立全局设计 Token（颜色/字体/圆角/阴影）
- [x] 在 `src/assets/main.css` 增加 CSS 变量（主色、语义色、中性色、圆角、阴影）。
- [x] 配置全局字体栈：中文优先 sans；`JetBrains Mono` 仅保留给数字/计时场景。
- [x] 增加工具类：`.mono`、统一卡片/按钮基础类（如 `.card-base`、`.btn-primary`）。
- [x] 补充 `prefers-reduced-motion` 降级规则。

### 2. 任务页语义统一（视觉与交互）
- [x] `TaskList` 勾选完成态从蓝色改为成功色（绿），保持清晰对比。
- [x] 优先级样式从 emoji 方案调整为“圆点 + 文本标签”。
- [x] 低优先级必须可见，不再“弱化到近似无状态”。
- [x] 任务项统一轻卡片化风格（10px 圆角、1px 边框、轻阴影）。

### 3. 响应式基础补齐（先覆盖主页面）
- [x] `TaskList`：`<768` 改单列，右侧详情改抽屉/覆盖层。
- [x] `Dashboard`：三列卡片在平板降为两列，手机单列。
- [x] `WeeklyReview`：主区与历史区在平板/手机改纵向堆叠。
- [x] `BasicLayout`：侧栏在平板/手机支持收起，避免内容区被挤压。

### 4. 文案与可读性清理
- [x] 统一页面最小可读字号为 12/13px。
- [x] 清理过度玩梗文案，改为行动导向提示。
- [x] 错误提示统一格式：问题 + 下一步建议（如“请检查网络后重试”）。

### 5. P0 验收
- [x] 桌面/平板/手机三端完成主流程走通（登录 -> 选项目 -> 任务编辑 -> AI 规划）。
- [x] 颜色与字体符合 `design.md` 定义，无明显风格漂移。
- [x] 动效在 `reduced-motion` 下可正常降级。

---

## P1：体验增强（P0 稳定后）

### 1. AI 拆解导入闭环增强
- [ ] `AiPlanner` 支持“草稿勾选后导入”，而非全量导入。
- [ ] 导入前提供结构化确认摘要（项目名、里程碑数、任务数）。
- [ ] 导入失败支持部分失败提示与重试策略。

### 2. 周报与任务数据联动
- [ ] `WeeklyReview` 显示本周任务来源摘要（完成数、重点项目）。
- [ ] 从周报快速跳转到对应项目/任务列表。

### 3. 交互一致性补齐
- [ ] 高风险操作加入撤销能力（Undo toast 5s）优先于仅确认弹窗。
- [ ] 全站 toast 样式与时长统一。

### 4. P1 验收
- [ ] AI 生成与导入流程无歧义，用户可控。
- [ ] 周报数据与任务数据一致，无统计口径冲突。

---

## P2：扩展能力（按需求逐项评估）

### 1. 新视图能力（一次只上线一个）
- [ ] 看板视图（四象限）+ 拖拽排序。
- [ ] 日历视图（月/周）+ 拖拽改期。
- [ ] 专注模式（单任务沉浸 + 计时）。

### 2. 评估与回滚策略
- [ ] 每个新视图独立灰度开关。
- [ ] 上线后跟踪使用率与留存，低使用功能可降级隐藏。

---

## 代码改动路径（预计）
- `src/assets/main.css`
- `src/layout/BasicLayout.vue`
- `src/views/task/TaskList.vue`
- `src/views/dashboard/Dashboard.vue`
- `src/views/review/WeeklyReview.vue`
- `src/views/ai/AiPlanner.vue`
- `src/views/LoginView.vue`
- `src/views/setting/Settings.vue`

---

## 追加待办（验收补充）

### A. 任务详情交互可用性
- [ ] `TaskList`：右侧详情中“优先级”整行可点击打开下拉（不仅限当前值按钮区域）。
- [ ] `TaskList`：右侧详情中“截止日期”整行可点击打开日期选择器。
- [ ] `TaskList`：右侧详情中“所属阶段”整行可点击打开下拉（不仅限 `select` 本体区域）。
- [ ] `TaskList`：截止日期禁止直接键盘编辑日期文本，仅允许通过日期选择器修改。

### B. 左侧导航信息分区清晰度
- [ ] `BasicLayout`：固定功能区增加左对齐灰色分组标题“功能”（位于“数据仪表盘”上方）。
- [ ] `BasicLayout`：清单区增加左对齐灰色分组标题“清单”（位于功能区分隔线下方）。
- [ ] `BasicLayout`：将底部独立“添加清单”按钮改为“清单”标题行右侧 `+` 按钮，减少冗余入口。
